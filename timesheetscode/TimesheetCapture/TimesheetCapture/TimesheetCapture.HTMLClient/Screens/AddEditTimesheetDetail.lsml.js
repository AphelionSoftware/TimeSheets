/// <reference path="../GeneratedArtifacts/viewModel.js" />

myapp.AddEditTimesheetDetail.created = function (screen) {
    // Write code here.
    /*screen.details.dataWorkspace.Timesheets_Data.PersonItem.filter("PersonName eq 'Active'").execute().then(function (x) {

        thisObject.setActiveType(x.results[0]);
    }, function (x) {
        msls.showMessageBox(x, {
            title: "Default value for ActiveType failed"
        });
    });
    */
};